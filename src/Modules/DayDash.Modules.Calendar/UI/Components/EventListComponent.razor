@inject IStringLocalizer<CalendarResources> Loc

<div class="event-list">
    <ul>
        @foreach (var calendarEvent in Events)
        {
            <li @onclick="(() => OnEventSelected.InvokeAsync(calendarEvent))">
                @if (calendarEvent.EventType is not null)
                {
                    <span class="event-dot" style="background-color:@calendarEvent.EventType.Color"></span>
                    <span class="event-type">@calendarEvent.EventType.Name</span>
                }
                <span>@calendarEvent.Title</span>
                @if (!calendarEvent.IsAllDay)
                {
                    <span>@calendarEvent.TimeFrom?.ToString("HH:mm")</span>
                }
            </li>
        }
    </ul>
</div>