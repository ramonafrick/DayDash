@inject IStringLocalizer<StudyPlannerResources> Loc

<h3>@Loc["TodayStudyPlan"]</h3>

<ul>
    @foreach (var plan in TodayPlans)
    {
        <li>@Loc["Study"]: @plan.Subject – @plan.DailyMinutes @Loc["Minutes"]</li>
    }
</ul>